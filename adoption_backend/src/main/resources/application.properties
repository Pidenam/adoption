spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/adoption_db}
spring.datasource.username=${DATABASE_USER:root}
spring.datasource.password=${DATABASE_PASSWORD:admin1234}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

server.port=${PORT:8080}

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
fichiers.dossier=${FICHIERS_DOSSIER:uploads/orphelins}
jwt.secret=${JWT_SECRET:cleSecreteParDefautPourLeDeveloppementLocalSeulement123456789}
jwt.expiration=${JWT_EXPIRATION:86400000}